
type Tibi @model 
@auth(rules: [
  
  ## Owners can do everything
  { allow: owner },
  
  # Admin users can access any operation.
  { allow: groups, groups: ["admin"] }

]) {
  id: ID!
  

  firstName: String!
  lastName: String! 
}